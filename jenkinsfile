pipeline {
    agent {
        label "master"
    }
    stages {
	stage('Build AMI') {
		steps {
		   println "Building AMI from the packer script.."
		   println ""
		   sh "/usr/local/bin/packer build build_packer_ami.json"
		}
	}
    }
}
