pipeline {
    agent {
        label "master"
    }
    stages {
	stage('Build AMI') {
		steps {
		   println "Building AMI from the packer script.."
		   println ""
		   sh "pwd"
		   sh "packer --version"
		}
	}
    }
}
